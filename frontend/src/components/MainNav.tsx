import { Button } from './ui/button'

const MainNav = () => {
	return (
		<Button variant="ghost" className="font-bold hover:text-red-500 hover:big-white font-poppins">
			Log In
		</Button>
	)
}

export default MainNav
